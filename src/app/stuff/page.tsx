import { GalleryStuffSelection } from "@/components/gallery/GalleryStuffSelection";

export const dynamic = "force-dynamic";

export default function StuffPage() {
  return <GalleryStuffSelection />;
}
